<section id="logs">
	<% if current_user.logs_today? %>
		<p class="lead">
			<%= current_user.hours_for_today %> hours logged today
		</p>
		<%= render "logs/logs", logs: @logs %>
	<% else %>
		<%= render "logs/blank_slate", text: "today" %>
	<% end %>
</section>
